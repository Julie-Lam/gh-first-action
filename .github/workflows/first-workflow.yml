name: First Workflow concurrency
on: workflow_dispatch #allows the user to manually trigger this workflow
jobs:
  first-job: #custom identifier for this job
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello World!"
      - name: Print goodbye
        run: echo "Done - bye!"
